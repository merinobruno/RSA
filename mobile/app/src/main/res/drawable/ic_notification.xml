<?xml version="1.0" encoding="utf-8"?>
<!-- Status bar icons must be a flat white silhouette on a transparent background; the system
     tints them itself, so no color other than white/transparent is used here. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="24"
    android:viewportHeight="24"
    android:tint="#FFFFFF">
    <path
        android:fillColor="#FF000000"
        android:pathData="M12,2 L17,20 L12,17 L7,20 Z" />
</vector>
